export enum WeaponStatusChange {
  Dropped,
  Reload,
  Empty,
  SilencerAttach,
  SilencerAttached,
  SilencerDetach,
  SilencerDetached,
  Bought,
  // Should not implement this one yet. Will be a lot of data and we should review if it is worth it or not
  Fire,
}
